# Exclude all top-level hidden directories by convention
/.*/

# Exclude tarballs generated by `npm pack`
/*.tgz

# Tests
__mocks__
__tests__

# Config files
/babel.config.js

# Android test directories
/android/src/androidTest/
/android/src/test/
/android/build/

# Example app (not needed in npm package)
/example/

# Source files (only publish compiled output)
/src/

# CLI source (only publish dist/)
/cli/bin/
/cli/commands/
/cli/server/
/cli/tunnel/
/cli/types/
/cli/tsconfig.json
/cli/package.json
/cli/node_modules/

# Plugin source (only publish build/)
/plugin/src/
/plugin/tsconfig.json

# Widget (needed for CLI, but exclude dev files)
/widget/node_modules/
/widget/package-lock.json
/widget/.expo/

# Dev files
/.context/
/tsconfig.json
/.eslintrc*
/.prettierrc*
